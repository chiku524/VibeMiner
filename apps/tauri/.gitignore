# Minisign updater keypair (private key → GitHub secret only; public key lives in tauri.conf.json)
*.key
vibeminer-updater.key.pub

# Generated by scripts/prepare-frontend-dist.cjs before `tauri build` (absolute window URLs for production)
src-tauri/tauri.bundle-urls.json
