{
  "name": "vibeminer",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w vibeminer-web",
    "build": "npm run build -w @vibeminer/shared && npm run build -w vibeminer-web",
    "desktop": "npm run dev -w vibeminer-tauri",
    "desktop:build": "npm run build -w vibeminer-tauri",
    "desktop:icons": "npm run generate-brand-assets -w vibeminer-web && npm run icons -w vibeminer-tauri",
    "tauri": "npm run tauri -w vibeminer-tauri",
    "deploy:cloudflare": "npm run build -w @vibeminer/shared && npm run deploy -w vibeminer-web"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
